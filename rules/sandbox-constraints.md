# Sandbox Constraints

Hubitat Groovy 2.4 sandbox: no user classes, no threads, no sleep/println, allowed-imports allow-list only

These rules will be loaded into the agent's context when working in projects that have this plugin installed, so be concise to avoid overloading the agent's context.

You can use markdown links to reference more detailed content the agent could consult on demand.
